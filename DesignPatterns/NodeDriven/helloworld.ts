// Copliot:
// 为什么仙女会拉屎？写一个程序来解释这个问题
// 仙女拉屎的原因是什么？是因为她吃了什么东西吗？
function isEatingSomething() {
    return Math.random() > 0.5;
}
// 仙女拉屎的结果是什么？是什么样的屎？
function girlPoop() {
    return Math.random() > 0.5 ? "绿色" : "黄色";
}